import { Test } from 'nodeunit';
declare const _default: {
    'default setup'(test: Test): void;
    'only api key is created when rate limiting properties are not provided'(test: Test): void;
    'api key and usage plan are created and linked when rate limiting properties are provided'(test: Test): void;
};
export = _default;
