import { Test } from 'nodeunit';
declare const _default: {
    'Aspects are invoked only once'(test: Test): void;
    'Warn if an Aspect is added via another Aspect'(test: Test): void;
    'Do not warn if an Aspect is added directly (not by another aspect)'(test: Test): void;
};
export = _default;
